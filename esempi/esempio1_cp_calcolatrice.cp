import time

// Calcolatrice in C Python

print.log("ciao questa è la tua calcolatrice in cpython")

getinput("per iniziare cliccare INVIO --")

string decimaleonoff getinput("vuoi usare i decimali?")

if (decimaleonoff == "si")
    float numero1 getinput("qual è il primo numero?" + f)
    float numero2 getinput("qual è il secondo numero?" + f)
    time.breakcode(1)
    print.output("la tua operazione fa:", numero1 + numero2)

if (decimaleonoff == "no")
    int numero1_non_decimale getinput("qual è il primo numero?" + f)
    int numero2_non_decimale getinput("qual è il secondo numero?" + f)
    time.breakcode(1)
    print.output("la tua operazione fa:", numero1_non_decimale + numero2_non_decimale)
